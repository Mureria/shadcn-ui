import React from "react";
import Hero from "../components/Hero";

const Examples = () => {
  return (
    <div>
      <Hero
        subhead="New components,cli updates and more"
        title="Check out some examples"
        paragraph="Dashboard, cards, authentication. Some examples built using the components. Use this as a guide to build your own."
        message="Get Started"
        mess="Components"
      />
    </div>
  );
};

export default Examples;
